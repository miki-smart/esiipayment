# yaml-language-server: $schema=https://spec.esiipayment.et/schema/cassette.v1.schema.json
#
# The request was sent but no response was ever received. Per Invariant
# I4, this must resolve to Processing + Poll, never Failed.
name: collect.timeout
operation: collect
seed:
  clock: "2026-01-15T09:30:00Z"
  uuid: []
  idempotency_key: "CHAPA-COLLECT-TIMEOUT-0001"
environment: sandbox
ctx:
  webhook_url: "https://merchant.example/webhooks/chapa"
  return_url: "https://merchant.example/return/chapa"
credentials:
  secret_key: "CHASECK_TEST-fixture-not-a-real-key"
intent:
  amount: 15000
  currency: ETB
  email: "customer@example.com"
  first_name: "Abebe"
  last_name: "Kebede"
transport_failure: true
interactions:
  - request:
      method: POST
      path: /v1/transaction/initialize
      headers: { Authorization: "Bearer CHASECK_TEST-fixture-not-a-real-key" }
      body: '{"amount":"150.00","currency":"ETB","email":"customer@example.com","first_name":"Abebe","last_name":"Kebede","tx_ref":"CHAPA-COLLECT-TIMEOUT-0001","callback_url":"https://merchant.example/webhooks/chapa","return_url":"https://merchant.example/return/chapa"}'
